[package]
name = "pcap_decode"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Decodes Robocol traffic out of a pcap using the robocol crate's own parsers"

[[bin]]
name = "pcap_decode"
path = "src/main.rs"

[dependencies]
robocol = { path = "../../robocol" }
serde_json = "1"
