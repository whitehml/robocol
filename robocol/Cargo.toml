[package]
name = "robocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "FTC Robocol (Driver Station <-> Robot Controller) UDP protocol client"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
