/target
Cargo.lock.orig

# Packet captures and decoded transcripts (kept out of the tree; only tooling
# is versioned). session.sh writes a directory per run.
capture/captures/*
!capture/captures/.gitkeep
